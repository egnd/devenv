---
- hosts: localhost
  connection: local
  tasks:
    - debug: msg='Install firefox'
    - block:
      - apt: 
          force_apt_get: yes
          name:
            - firefox
      when: ansible_os_family == 'Debian' # @TODO: ubuntu
# @TODO: manjaro
# @TODO: fedora
